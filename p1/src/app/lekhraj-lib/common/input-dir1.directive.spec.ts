import { InputDir1Directive } from './input-dir1.directive';

describe('InputDir1Directive', () => {
  it('should create an instance', () => {
    const directive = new InputDir1Directive();
    expect(directive).toBeTruthy();
  });
});
